<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.AppCompat.Light.NoActionBar" parent="Theme.AppCompat.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>
    
    <!-- 透明悬浮主题 - 用于字幕条（新增，推荐使用） -->
    <style name="TransparentFloatingTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 必须透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
        <!-- 禁用系统背景绘制 -->
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        <!-- 不显示状态栏/导航栏遮挡 -->
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <!-- 其他必要属性 -->
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    
    <!-- Translucent theme for transparent background (保留旧主题) -->
    <style name="Theme.AppCompat.Light.NoActionBar.Translucent" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
</resources>
